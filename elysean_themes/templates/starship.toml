# elysean_themes/templates/starship.toml


format = """
╭─\
[](fg:{{ colors.ACCENT_LOW }})\
[$os$username](bold bg:{{ colors.ACCENT_LOW }} fg:{{ colors.BG_DARK }})\
[](fg:{{ colors.ACCENT_LOW }})\
[ ](fg:{{ colors.ANSI_BLUE }})\
$directory\
[](fg:{{ colors.ANSI_BLUE }})\
$fill\
$git_branch\
$git_status\
\n╰──\
$character"""

[fill]
symbol = "·"
style = "fg:{{ colors.BG_HIGHLIGHT }}"

[character]
success_symbol = "[❯](bold fg:{{ colors.ANSI_BLUE }})"
error_symbol = "[❯](bold fg:{{ colors.ANSI_RED }})"

[os]
disabled = false
style = "bold bg:{{ colors.ACCENT_LOW }} fg:{{ colors.BG_DARK }}"

[os.symbols]
Arch = " "
Ubuntu = " "
Fedora = " "
NixOS = " "
Debian = " "
CachyOS = " "

[hostname]
ssh_only = false
trim_at = "."
style = "bold fg:{{ colors.BG_DARK }} bg:{{ colors.ACCENT_LOW }}"
format = "[$hostname]($style)"

[username]
show_always = true
format = "[$user](bold bg:{{ colors.ACCENT_LOW }} fg:{{ colors.BG_DARK }})"

[directory]
style = "bold fg:{{ colors.FG }} bg:{{ colors.ANSI_BLUE }}"
format = "[󰉖 󱦰 $path]($style)"
truncation_length = 5
truncation_symbol = "../"
home_symbol = "~"
truncate_to_repo = false

[directory.substitutions]
"Documents" = "󰈙 "
"Downloads" = " "
"Music" = " "
"Pictures" = " "
"~" = " "

[git_branch]
symbol = " "
style = "bg:{{ colors.BG_HIGHLIGHT }}"
format = '[](fg:{{ colors.ANSI_BLUE }})[[ $symbol$branch ](bold fg:{{ colors.FG }} bg:{{ colors.ANSI_BLUE }})]($style)'

[git_status]
style = "bg:{{ colors.ANSI_BLUE }}"
format = '[[($all_status$ahead_behind )](bold fg:{{ colors.FG }} bg:{{ colors.ANSI_BLUE }})]($style)[](fg:{{ colors.ANSI_BLUE }})'

[nodejs]
symbol = " "
style = "bg:{{ colors.BG_STRIPE }}"
format = '[[ $symbol ($version) ](fg:{{ colors.ANSI_BLUE }} bg:{{ colors.BG_STRIPE }})]($style)'

[rust]
symbol = " "
style = "bg:{{ colors.BG_STRIPE }}"
format = '[[ $symbol ($version) ](fg:{{ colors.ANSI_BLUE }} bg:{{ colors.BG_STRIPE }})]($style)'

[golang]
symbol = " "
style = "bg:{{ colors.BG_STRIPE }}"
format = '[[ $symbol ($version) ](fg:{{ colors.ANSI_BLUE }} bg:{{ colors.BG_STRIPE }})]($style)'

[php]
symbol = " "
style = "bg:{{ colors.BG_STRIPE }}"
format = '[[ $symbol ($version) ](fg:{{ colors.ANSI_BLUE }} bg:{{ colors.BG_STRIPE }})]($style)'

[python]
symbol = " "
style = "bg:{{ colors.BG_STRIPE }}"
format = '[[ $symbol($version) ](bold fg:{{ colors.ANSI_BLUE }} bg:{{ colors.BG_STRIPE }})]($style)'
